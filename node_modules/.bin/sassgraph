#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node --max-old-space-size=2048"  "$basedir/../sass-graph/bin/sassgraph" "$@"
  ret=$?
else 
  node --max-old-space-size=2048  "$basedir/../sass-graph/bin/sassgraph" "$@"
  ret=$?
fi
exit $ret

