#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node --max-old-space-size=2048"  "$basedir/../uglify-js/bin/uglifyjs" "$@"
  ret=$?
else 
  node --max-old-space-size=2048  "$basedir/../uglify-js/bin/uglifyjs" "$@"
  ret=$?
fi
exit $ret

